name: PyNE

on:
  push:
    branches: [ "master", "main" ]
  pull_request:
    branches: [ "master", "main" ]
  workflow_dispatch:
    branches: [ "master", "main" ]

permissions:
  contents: read

jobs:
    job-1:
        runs-on: ubuntu-latest
        container:
            image: python:2.7.16
        call-workflow:
            uses: ./.github/workflows/run_tests.yml
   job-2:
        runs-on: ubuntu-latest
        container:
            image: python:3.10.13
        call-workflow:
            uses: ./.github/workflows/run_tests.yml